{
  "name": "webcomponents-workspace",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev:demo": "pnpm --filter demo dev"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}